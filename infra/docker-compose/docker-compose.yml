# DEPRECATED: Use compose.yaml instead.
# This file is kept as a pointer for backward compatibility.
# The canonical local topology is defined in compose.yaml.
#
# Start with:
#   podman compose -f infra/docker-compose/compose.yaml up -d
