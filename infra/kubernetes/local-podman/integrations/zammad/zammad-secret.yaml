apiVersion: v1
kind: Secret
metadata:
  name: zammad-db
  namespace: supportplane-integrations
  labels:
    app.kubernetes.io/name: zammad
    app.kubernetes.io/part-of: supportplane
    supportplane.local/non-production: "true"
    supportplane.local/scope: local-sandbox-only
type: Opaque
stringData:
  # LOCAL DEV PLACEHOLDER ONLY
  # Note: Zammad Docker entrypoint uses POSTGRESQL_PASS
  POSTGRESQL_PASS: "zammad_dev_password"
