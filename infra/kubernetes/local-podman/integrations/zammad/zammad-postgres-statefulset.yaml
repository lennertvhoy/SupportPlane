apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: zammad-postgres
  namespace: supportplane-integrations
  labels:
    app.kubernetes.io/name: zammad-postgres
    app.kubernetes.io/part-of: supportplane
spec:
  serviceName: zammad-postgres
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: zammad-postgres
  template:
    metadata:
      labels:
        app.kubernetes.io/name: zammad-postgres
        app.kubernetes.io/part-of: supportplane
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          env:
            - name: POSTGRES_DB
              value: zammad
            - name: POSTGRES_USER
              value: zammad
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: zammad-postgres
                  key: password
          ports:
            - containerPort: 5432
              name: postgres
          livenessProbe:
            exec:
              command: ["pg_isready", "-U", "zammad"]
            initialDelaySeconds: 15
            periodSeconds: 15
            failureThreshold: 5
          readinessProbe:
            exec:
              command: ["pg_isready", "-U", "zammad"]
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: zammad-postgres-data
