# Local sandbox only. Not production hardened.
apiVersion: v1
kind: Secret
metadata:
  name: keycloak-local-dev
  namespace: supportplane-integrations
  labels:
    app.kubernetes.io/name: keycloak
    app.kubernetes.io/part-of: supportplane
    supportplane.local/non-production: 'true'
    supportplane.local/scope: local-sandbox-only
type: Opaque
stringData:
  # LOCAL DEV PLACEHOLDER ONLY — NOT A PRODUCTION SECRET
  admin-username: 'admin'
  admin-password: 'keycloak-local-admin-password'
  db-password: 'keycloak-local-db-password'
