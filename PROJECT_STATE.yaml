# PROJECT_STATE.yaml - Structured current truth

metadata:
  updated_at: 2026-04-28T19:30:00+02:00
  updated_by: agent
  version: "statedd-template-v3"

workflow:
  repo_mode: operating
  bootstrap:
    completed: true
    completed_on: 2026-04-26
    system_investigated: true
    repo_investigated: true
    user_intake_complete: true
    completion_summary: >
      Product architecture, target users, MVP sequence, core safety model,
      implementation stack, external integration order, backlog, active queue,
      and evidence references were captured from user-provided architecture and
      direct local/source verification.
    unknowns_remaining:
      - exact repository hosting/branching model not configured
      - production deployment target not selected beyond local Docker Compose first
      - concrete Zammad instance credentials and schema constraints not configured
      - AI provider, model names, and retention defaults not selected
      - legal/compliance requirements need counsel review before market claims
      - evidence bundle object storage persistence not implemented
      - evidence bundle cryptographic signing not implemented

verification_labels:
  observed: verified directly in the current session
  unknown: not yet determined from currently available evidence
  reported: provided by the user or prior context, not independently proven
  blocked: verification attempted but prevented
  assumed: temporary working assumption
  stale: previously verified but no longer fresh
  invalid: known false or superseded

current_state:
  repository:
    canonical_path: /home/ff/Documents/Projects/SupportPlane
    path_status: observed
    git_repository: true
    branch: main
    head: 36e51607bee95232bec4f7d49f3aea67b4937053
    worktree_status: clean
    notes:
      - git repository initialized during BL-001
      - BL-002 contracts and schema committed
      - BL-003 mock-first API slice committed
      - BL-004 support cockpit UI shell committed and closure verified
      - BL-005 mock AI gateway and draft suggestion workflow implemented and committed

  operating_mode:
    status: observed
    mode: operating
    summary: >
      Bootstrap is complete. BL-002 contracts/schema, BL-003 mock-first API
      slice, BL-004 support cockpit UI shell, and BL-005 mock AI draft
      suggestion workflow are implemented. The repository contains a NestJS API,
      Next.js web app, and PostgreSQL-backed local persistence path with
      browser-verified panels. External ticketing writeback, real AI provider
      calls, and production delivery queues are not implemented.

  project:
    name: SupportPlane
    type: governed_ai_support_cockpit
    lifecycle_stage: mvp_foundation
    foundation_history:
      completed_items: [BL-002, BL-003, BL-004, BL-005, BL-006, BL-007, BL-008, BL-009, BL-018, BL-020, BL-041, BL-042, BL-043, BL-044, BL-045, BL-046, BL-047, BL-048, BL-049, BL-050, BL-091, BL-092, BL-093, BL-094, BL-095, BL-097, BL-098]
      summary: >
        Early slices established contracts/schema, mock-first API, cockpit UI,
        mock AI gateway, local Podman-compatible topology, Zammad connector
        boundary, evidence bundle skeleton, and fake incoming call flow.
      detail_location: WORKLOG.md
    bl_041_status:
      completed: true
      status: closure_ready
      scope: automatic_support_session_creation_from_incoming_call_events
      screenshot_folder: unknown
      screenshot_count: 0
      final_closure_commit: unknown

    bl_042_status:
      completed: true
      status: closure_ready
      scope: suggested_greeting_generation_from_call_plus_ticket_context
      screenshot_folder: unknown
      screenshot_count: 0
      final_closure_commit: unknown

    bl_043_status:
      completed: true
      status: closure_ready
      scope: call_console_ui_closure
      screenshot_folder: unknown
      screenshot_count: 0
      final_closure_commit: unknown

    bl_045_status:
      completed: true
      status: closure_ready
      scope: call_recording_attachment_and_playback_mock_foundation
      screenshot_folder: output/playwright/session-045-call-recording-mock-final-closure/
      screenshot_count: 8
      final_closure_commit: unknown

    bl_047_status:
      completed: true
      status: closure_ready
      scope: explicit_sharing_state_storage_and_visible_sharing_indicator
      screenshot_folder: output/playwright/session-047-049-screen-context-hardening/
      screenshot_count: 0
      final_closure_commit: unknown

    bl_048_status:
      completed: true
      status: closure_ready
      scope: deterministic_active_window_metadata_and_manual_screenshot_metadata_capture
      screenshot_folder: output/playwright/session-047-049-screen-context-hardening/
      screenshot_count: 0
      final_closure_commit: unknown

    bl_049_status:
      completed: true
      status: closure_ready
      scope: enhanced_redaction_and_structured_observation_fields_in_evidence_bundle
      screenshot_folder: output/playwright/session-047-049-screen-context-hardening/
      screenshot_count: 0
      final_closure_commit: unknown

    bl_046_status:
      completed: true
      status: closure_ready
      scope: operator_companion_screen_observations_during_active_calls
      screenshot_folder: output/playwright/session-046-operator-companion-closure-canonical/
      screenshot_count: 9
      final_closure_commit: unknown

    bl_050_status:
      completed: true
      status: closure_ready
      scope: postgres_persistence_foundation_prisma_store_runtime_switching
      screenshot_folder: unknown
      screenshot_count: 0
      final_closure_commit: unknown

    bl_044_status:
      completed: true
      status: closure_ready
      scope: telephony_adapter_contract_and_bridge_boundary
      screenshot_folder: output/playwright/session-043-call-console-ui-final-closure/
      screenshot_count: 8
      final_closure_commit: unknown

  runtime_identity:
    status: api_and_web_runtime_verified
    repo_path: /home/ff/Documents/Projects/SupportPlane
    branch: main
    head: 89a8590dacfff01646b2459228bf7734df6aefff
    process_or_container:
      - node process (NestJS) on port 4110
      - node process (Next.js) on port 3200
    port_or_base_url:
      - http://localhost:4110
      - http://localhost:3200
    rebuilt_in_current_slice: true
    duplicate_runtimes_checked: true
    notes:
      - NestJS API verified via integration tests and curl
      - Next.js web app verified via browser screenshots
      - BL-050 PostgreSQL persistence verified with restart-survival script
      - Infra containers verified via podman-compose with healthchecks
    bl_092_status:
      completed: true
      status: closure_complete_after_repair
      scope: durable_action_outbox_workflow_foundation
      backlog_id: BL-092
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260427234000_durable_action_outbox_workflow/
      docs:
        - docs/ACTION_OUTBOX_WORKFLOW.md
        - docs/PERSISTENCE.md
        - docs/EVIDENCE_BUNDLES.md
        - docs/SUPPORT_CASE_WORKFLOW.md
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
      new_api_endpoints:
        - GET /support-sessions/:id/actions
        - POST /support-sessions/:id/actions
        - GET /actions/:id
        - POST /actions/:id/submit-for-review
        - POST /actions/:id/approve
        - POST /actions/:id/reject
        - POST /actions/:id/queue
        - POST /actions/:id/mock-deliver
        - POST /actions/:id/cancel
        - GET /outbox
        - GET /outbox/:id
        - POST /outbox/:id/retry
        - POST /outbox/:id/mock-deliver
      screenshot_folder: output/playwright/session-092-durable-action-outbox-workflow-final-closure/
      screenshot_count: 17
      validation: full_gate_passed_with_npm_audit_vulnerabilities_reported
      implementation_commit: 6819301fa5af04a6b02bbe6af532ae669e7a880a
      closure_repair_commit: 5d0a9c54bd56e714da75bcfe84b8a809a417f6d8
      final_closure_commit: 4c7697de0f143cba09ec60c9f1de05725ec659c7
      closure_repair_summary: >
        Fixed lifecycle contradiction where UI showed mock_delivered attempt history
        on draft/review_required actions. Two bugs: backend returned all session outbox
        items regardless of action status; frontend picked outboxItems[0] for attempts.
      final_closure_summary: >
        Completed 17-screenshot audit proof set, fixed verify_postgres_persistence.sh
        to handle occupied API ports, reran full validation gate, updated acceptance freeze
        and all state docs, and added closure repair rule to AGENTS.md.
      notes:
        - action/outbox delivery is local mock only with realNetwork=false and externalWriteAttempted=false
        - viewer can inspect but cannot mutate; operator can create/submit; admin can approve/reject/queue
        - backend suppresses outbox items until queued/delivered/failed; frontend scopes attempt history by supportActionId
    bl_094_status:
      completed: true
      status: closure_complete_after_governance_repair
      scope: connector_writeback_readiness_gates_and_delivery_policy_controls
      backlog_id: BL-094
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260428094012_delivery_policy_controls/
      docs: [docs/DELIVERY_POLICY_CONTROLS.md]
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
      new_api_endpoints:
        - GET /delivery-policies
        - GET /delivery-policies/:id
        - PATCH /delivery-policies/:id
        - POST /delivery-policies/:id/validate
        - POST /delivery-policies/:id/connector-readiness
      screenshot_folder: output/playwright/session-095-bl094-final-closure-max20/
      screenshot_count: 20
      validation: full_gate_passed_with_npm_audit_vulnerabilities_reported
      governance_repair:
        reason: prior closure violated AGENTS.md screenshot cap (24 screenshots) and stale BACKLOG.md
        actions:
          - AGENTS.md updated with hard 20-screenshot cap and backlog currency rule
          - BACKLOG.md reconciled with accepted/superseded/partial markers for BL-001 through BL-094
          - Old screenshot folder session-094-delivery-policy-controls-final-closure/ deleted
          - New canonical folder session-095-bl094-final-closure-max20/ created with 20 unique screenshots
      final_closure_commit: 93afe787847964b666e502d083b0dbc63cc79d86
      notes:
        - delivery policy evaluation gates (in order): killSwitch → enabled → allowedActionTypes → approvalRequired → minimumApproverRole → requireHumanReview → requireEvidenceBundle → requireConnectorValidation
        - all policy decisions return realNetworkAllowed=false, writebackEnabled=false, externalWriteAllowed=false
        - real writeback toggle requests return 400 with explicit rejection
        - viewer sees read-only policy panel; admin can update safe fields only
        - policy decisions are stored in outbox item deliveryIntent and audit events
        - ActionsService.queue() evaluates policy before creating outbox item
        - ActionsService.processClaimedOutbox() re-evaluates policy before processing
        - evidence bundles include policy provenance
        - no real external writeback, email, telephony, AI provider, external broker, object storage, raw media, production audit immutability, compliance claim, SSO/OAuth/SAML/OIDC, MFA, or password reset
    bl_095_status:
      completed: true
      status: closure_complete_after_repair
      scope: connector_installation_settings_foundation
      backlog_id: BL-095
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260428131300_bl095_connector_installation_settings/
      docs:
        - docs/TICKET_CONTEXT_CONNECTOR_SAFETY.md
        - docs/ZAMMAD_CONNECTOR.md
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
      new_api_endpoints:
        - POST /connector-installations
        - PATCH /connector-installations/:id
        - POST /connector-installations/:id/validate
        - POST /connector-installations/:id/test
      screenshot_folder: output/playwright/session-096-bl095-connector-installation-settings-final-closure/
      screenshot_count: 14
      validation: full_gate_passed_with_npm_audit_vulnerabilities_reported
      closure_repair:
        reason: prior closure had wrong screenshot folder name (session-095 conflicted with BL-094), incomplete validation gate, missing 18-section handoff, short commit hash, and under-proven cross-tenant/server-side denial evidence
        actions:
          - Deleted old session-095-connector-installation-settings/ folder
          - Created canonical session-096-bl095-connector-installation-settings-final-closure/ with 14 unique screenshots
          - Added scripts/bl095_screenshots.js with hard max-20 cap and duplicate detection
          - Updated docs/TICKET_CONTEXT_CONNECTOR_SAFETY.md with current endpoint truth and credential handling note
          - Ran full validation gate with exact command results
      final_closure_commit: d42b226fbef59eb078675a1b001b17a9038d12d2
      notes:
        - connector installation settings are editable by admin/operator; viewer denied server-side with 403
        - config secrets redacted to [REDACTED] in all GET responses and evidence bundles
        - credential/config JSON storage is local/mock/dev-only; not production credential management
        - cross-tenant connector installation access returns 404
        - connector readiness returns readyForRealWriteback=false and realNetwork=false
        - delivery policy controls still deny real writeback
        - no real production Zammad writeback, email, telephony, AI provider, external broker, object storage, raw media, production audit immutability, compliance claim, SSO/OAuth/SAML/OIDC, MFA, or password reset
    bl_097_status:
      completed: true
      status: closure_ready
      scope: connector_credential_reference_secret_broker_foundation
      backlog_id: BL-097
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260428160000_bl097_credential_reference_foundation/
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
      new_api_endpoints:
        - POST /credential-references
        - GET /credential-references
        - GET /credential-references/:id
        - PATCH /credential-references/:id
        - POST /connector-installations/:id/link-credential
        - POST /connector-installations/:id/unlink-credential
      new_audit_events:
        - credential_reference_created
        - credential_reference_updated
        - credential_reference_linked
        - credential_reference_unlinked
      screenshot_folder: output/playwright/session-097-credential-reference-foundation-final-closure/
      screenshot_count: 6
      validation: full_gate_passed
      final_closure_commit: b6b42ce571edbccb742eaff447338afb622aa8a3
      notes:
        - credential references are tenant-scoped and stored in PostgreSQL via Prisma
        - all API responses redact secretRef to [REDACTED]; raw secret values never leave the server
        - evidence bundles include credential reference summaries with metadata only (no secrets)
        - admin/operator can link/unlink credential references to connector installations
        - viewer sees read-only credential reference list; server-side 403 on write operations
        - secretRef values are local-dev opaque placeholders; no production credential broker exists
        - no real production Zammad writeback, email, telephony, AI provider, external broker, object storage, raw media, production audit immutability, compliance claim, SSO/OAuth/SAML/OIDC, MFA, or password reset
    bl_098_status:
      completed: true
      status: closure_ready
      scope: connector_runtime_configuration_credential_reference_readiness_foundation
      backlog_id: BL-098
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
      new_contracts:
        - packages/contracts/src/connector-runtime.ts
      new_api_endpoints:
        - GET /connector-installations/:id/config-schema
        - POST /connector-installations/:id/validate-config
        - POST /connector-installations/:id/runtime-readiness
        - GET /connector-installations/runtime/resolve
      new_audit_events:
        - connector_config_validated
        - connector_readiness_checked
        - connector_runtime_resolved
      screenshot_folder: output/playwright/session-099-bl098-closure-repair-final/
      screenshot_count: 15
      validation: full_gate_passed
      final_closure_commit: 36e51607bee95232bec4f7d49f3aea67b4937053
      notes:
        - config validation enforces mock-only safety: rejects mockMode:false, rejects unsafe real-network fields
        - runtime readiness returns mockReady based on mockMode && enabled; realReady is always false
        - runtime resolver returns tenant-scoped installation with credential reference metadata (no secretRef exposure)
        - evidence bundles include connector installation summaries with realNetwork:false, writebackEnabled:false, externalWriteAttempted:false
        - viewer denied server-side on config validation and readiness mutations with 403
        - cross-tenant access returns 404 on all runtime endpoints
        - all behavior remains local/mock-only with visible UI warnings
        - seed is idempotent via upsert with fixed IDs; exactly 1 credential ref linked to conn-inst-dev-001
        - ticket context panel shows connector runtime provenance card (installation name, type, mode, network status, linked credential count, capabilities)
        - old session-098 screenshot folder superseded and deleted per repair rules
        - no real production Zammad writeback, email, telephony, AI provider, external broker, object storage, raw media, production audit immutability, compliance claim, SSO/OAuth/SAML/OIDC, MFA, or password reset
    bl_093_status:
      completed: true
      status: closure_ready
      scope: background_outbox_worker_retry_deadletter_foundation
      backlog_id: BL-093
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      worker_app: apps/worker
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260428120000_outbox_worker_retry_deadletter_foundation/
      docs: [docs/OUTBOX_WORKER_OPERATIONS.md, docs/ACTION_OUTBOX_WORKFLOW.md, docs/PERSISTENCE.md]
      runtime:
        store_mode: postgres
        auth_mode: local
        api_url: http://localhost:4110
        web_url: http://localhost:3200
        postgres: localhost:5434
        worker_mode: local_mock_worker/process-once
      new_api_endpoints: [GET /outbox/worker/status, POST /outbox/process-once, POST /outbox/:id/retry, POST /outbox/:id/cancel, POST /outbox/:id/dead-letter]
      screenshot_folder: output/playwright/session-093-outbox-worker-retry-deadletter-foundation/
      screenshot_count: 24
      validation: full_gate_passed_with_npm_audit_vulnerabilities_reported
      final_closure_commit: f5c101da467384bce940f886f0e8226478180bfa
      notes:
        - worker/process-once delivery mode is local mock only; all results include mode=mock, realNetwork=false, writebackEnabled=false, externalWriteAttempted=false
        - retryable failures schedule retry; non-retryable or exhausted attempts move to dead_lettered
        - viewer can inspect only; admin/owner can process, retry, cancel, and dead-letter
        - forged role headers ignored; cross-tenant outbox access denied server-side
        - evidence bundles include attempt provenance without secrets or raw media
    bl_091_status:
      completed: true
      scope: support_case_workflow_foundation
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      new_modules:
        - apps/api/src/tickets/
      new_ui_components:
        - apps/web/components/TicketSummaryPanel.tsx
        - apps/web/components/CaseTimelinePanel.tsx
        - apps/web/components/SupportNoteDraftPanel.tsx
      updated_ui_components:
        - apps/web/components/ConnectorPanel.tsx
        - apps/web/app/page.tsx
        - apps/web/lib/api.ts
      new_api_endpoints:
        - GET /tickets
        - GET /tickets/:id
        - PATCH /connector-installations/:id
        - POST /connector-installations/:id/validate
        - POST /connector-installations/:id/test
        - POST /support-sessions/:id/support-note-drafts
      validation: all_typechecks_pass_111_api_tests_pass_browser_proof_20_screenshots
      screenshot_folder: output/playwright/session-091-support-case-workflow-foundation/
      screenshot_count: 20
      notes:
        - internal_note_drafts is represented in prisma/schema.prisma and committed migration prisma/migrations/20260427124815_init_persistence_foundation/migration.sql; npx prisma validate and migrate status passed on 2026-04-27
        - All new behavior is deterministic local/mock-only with visible UI warnings
        - No real Zammad, telephony, AI provider, queue, object storage, SSO, MFA, or password reset implemented
    bl_020_status:
      completed: true
      scope: ticket_context_connector_safety_foundation
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260427160804_ticket_context_connector_safety_foundation/
      new_contracts:
        - packages/contracts/src/customer.ts
        - packages/contracts/src/connector-installation.ts
      new_modules:
        - apps/api/src/customers/
        - apps/api/src/connector-installations/
      new_ui_components:
        - apps/web/components/CustomerReferencePanel.tsx
      updated_ui_components:
        - apps/web/components/ConnectorPanel.tsx
        - apps/web/components/EvidenceBundlePanel.tsx
        - apps/web/lib/api.ts
      validation: all_typechecks_pass_102_api_tests_pass_browser_proof_13_screenshots
      notes:
        - All new entities default to mockDevOnly: true
        - No real production Zammad, telephony, AI, or object storage implemented
    bl_018_status:
      completed: true
      scope: local_auth_rbac_tenant_boundary_foundation
      contracts_package: packages/contracts
      api_app: apps/api
      web_app: apps/web
      prisma_schema: prisma/schema.prisma
      migration: prisma/migrations/20260427152000_local_auth_rbac/
      docs:
        - docs/AUTHORIZATION.md
        - docs/LOCAL_DEVELOPMENT.md
        - docs/PERSISTENCE.md
      runtime:
        store_mode: postgres
        auth_modes: [dev, local]
        local_auth: PostgreSQL-backed local users, password hashes, sessions, roles, permissions
        seed_users:
          - admin@supportplane.local / dev-tenant / admin
          - operator@supportplane.local / dev-tenant / operator
          - viewer@supportplane.local / dev-tenant / viewer
          - admin@alt.supportplane.local / alt-tenant / admin
          - operator@alt.supportplane.local / alt-tenant / operator
        verification_script: scripts/verify_local_auth_rbac.sh
        screenshot_folder: output/playwright/session-018-auth-rbac-tenant-boundary-foundation/
        screenshot_count: 13
        limitations:
          - local MVP auth only; no SSO/OAuth/SAML/OIDC
          - no production password policy/MFA/rate limiting/password reset
          - audit integrity is not production immutable

  environment:
    status: observed
    host:
      os: Fedora Linux 43 Workstation Edition
      kernel: Linux 6.19.13-200.fc43.x86_64
      shell: /bin/bash
      terminal: dumb
      timezone: Europe/Brussels
    package_managers:
      - dnf
      - npm_or_corepack_not_verified
    language_runtimes:
      python: 3.14.4
      node: 22.22.0
      go: unknown
      rust: unknown
    container_tooling:
      podman: 5.8.2
      podman_compose: 1.5.0
      docker: absent
      compose_file: infra/docker-compose/compose.yaml
      tested_orchestrator: podman_compose
      docker_compose_expected_compatible: true
      docker_compose_verified: false
    browser_tooling:
      chrome: /usr/bin/google-chrome-stable
    ports:
      listeners_observed: true
      notes:
        - active listeners were observed with ss -ltn
        - none were tied to a SupportPlane runtime

  repository_structure:
    status: observed
    current_top_level_entries:
      - AGENTS.md
      - BACKLOG.md
      - NEXT_ACTIONS.md
      - PROJECT_STATE.yaml
      - README.md
      - STATUS.md
      - WORKLOG.md
      - docs
      - fixtures
      - infra
      - scripts
      - apps
      - packages
    current_entrypoints:
      - scripts/init_template.py
      - scripts/check_state_docs.py
      - scripts/health.js
      - apps/api/dist/src/main.js
    current_test_setup:
      - scripts/check_state_docs.py
      - python3 -m py_compile scripts/check_state_docs.py scripts/init_template.py
      - npm run typecheck --workspaces --if-present
      - npm run health
    planned_monorepo:
      apps:
        - apps/web
        - apps/api
        - apps/worker
        - apps/operator-companion
        - apps/endpoint-agent
      packages:
        - packages/contracts
        - packages/policy
        - packages/connectors
        - packages/ai
        - packages/audit
        - packages/ui
      infra:
        - infra/docker-compose
        - infra/kubernetes
        - infra/terraform-later
    deployment_assumptions:
      - MVP local self-hosting through Docker Compose or Podman-compatible compose
      - production later may use Kubernetes, managed PostgreSQL, NATS cluster, object storage, and OpenTelemetry
    contradictions:
      - repository still contains template fixtures in some apps
      - AGENTS template language still includes some generic workflow framing
      - apps/web and apps/worker are scaffold placeholders without frameworks installed yet
      - apps/api supports both in-memory and PostgreSQL stores via SUPPORTPLANE_STORE env var; PrismaStore uses Prisma v7.8.0 with pg adapter
      - packages/ai contains only deterministic mock provider behavior, not real provider integrations

  target_architecture:
    frontend:
      app: apps/web
      stack:
        - Next.js
        - React
        - TypeScript
        - Tailwind
        - shadcn/ui
        - OpenAPI-generated client
      primary_screens:
        - support cockpit
        - ticket context view
        - call console
        - device console
        - evidence bundle viewer
        - admin
    backend:
      app: apps/api
      stack:
        - NestJS
        - TypeScript
        - PostgreSQL
        - Prisma
        - NATS
        - OpenAPI
        - OpenTelemetry
      responsibilities:
        - auth
        - tenant isolation
        - RBAC and ABAC
        - support sessions
        - ticket context
        - call events
        - AI orchestration
        - connector gateway
        - policy checks
        - approval workflows
        - audit events
        - evidence bundles
    worker:
      app: apps/worker
      responsibilities:
        - async jobs
        - connector sync
        - evidence generation
        - AI/background workflows
    operator_companion:
      app: apps/operator-companion
      stack:
        - Tauri
        - Rust
        - TypeScript
      default_capture_mode: active_window_only
      status: planned_after_mvp_2
    endpoint_agent:
      app: apps/endpoint-agent
      language: Go
      connection_model: outbound_only_websocket_or_mtls
      v1_limitations:
        - read-only diagnostics
        - no arbitrary shell execution
      status: planned_after_operator_companion

  domain_model:
    central_aggregate: SupportSession
    important_entities:
      - Tenant
      - User
      - Role
      - Permission
      - Policy
      - ApprovalRule
      - ConnectorInstallation
      - SupportSession
      - TicketReference
      - CustomerReference
      - CallEvent
      - Device
      - EndpointAgent
      - OperatorCompanion
      - ScreenObservation
      - AIConversation
      - AIContextPacket
      - ToolManifest
      - ToolInvocation
      - ApprovalRequest
      - AuditEvent
      - EvidenceBundle
      - KnowledgeSource
      - ModelPolicy
      - SecretReference

  safety_model:
    authority_chain:
      - user role
      - tenant policy
      - device policy
      - tool manifest
      - risk level
      - approval rules
      - audit rules
      - execution gateway
    ai_authority: non_authoritative
    ai_allowed_to:
      - reason
      - summarize
      - suggest
      - draft
      - request policy-checked actions
    ai_not_allowed_to:
      - directly execute privileged work
      - access raw secrets
      - use hidden tools
      - bypass approval or audit
    mvp_tool_scope:
      - read_only
      - low_risk_with_approval_support

  external_integrations:
    ticketing_first:
      provider: Zammad
      status: planned
      evidence_ref: EV-2026-04-26-001
    ticketing_second:
      provider: GLPI
      status: planned
      evidence_ref: EV-2026-04-26-002
    phone_first:
      provider: simulated webhook
      status: planned_before_pbx
    phone_later:
      provider: Asterisk or FreePBX
      status: planned
      evidence_ref: EV-2026-04-26-003
    remote_support_first:
      provider: MeshCentral
      status: planned
      evidence_ref: EV-2026-04-26-004
    firewall_context:
      provider: Fortinet UI screen context first, API later
      status: planned
      notes:
        - no deep Fortinet API integration in MVP

  mvp_sequence:
    mvp_1:
      name: Ticket-aware AI cockpit
      status: active_target
      scope:
        - Zammad connector boundary
        - SupportSession
        - AIContextPacket
        - AI chat
        - ticket summary
        - draft internal note
        - write note back to Zammad
        - audit log
    mvp_2:
      name: Call simulator
      status: planned
    mvp_3:
      name: Operator Companion
      status: planned
    mvp_4:
      name: Endpoint Agent read-only diagnostics
      status: planned
    mvp_5:
      name: Approval-gated low-risk remediation
      status: planned

  backlog:
    status: observed
    scope: complete_milestone_level
    source: BACKLOG.md
    total_items: 90
    active_queue_source: NEXT_ACTIONS.md
    operating_loop_prompt: prompts/OPERATING_LOOP_START_PROMPT.md
    coverage:
      - foundation
      - ticket-aware AI cockpit
      - call simulator
      - operator companion
      - endpoint agent read-only diagnostics
      - approval-gated remediation
      - post-MVP integrations
      - admin governance and compliance evidence
      - production hardening

  evidence:
    status: active
    ledger: docs/EVIDENCE_LOG.md
    acceptance_freezes: docs/ACCEPTANCE_FREEZES.md
    artifact_root: docs/evidence
    source_refs:
      - EV-2026-04-26-001
      - EV-2026-04-26-002
      - EV-2026-04-26-003
      - EV-2026-04-26-004
      - EV-2026-04-26-005

  documentation:
    status: observed
    primary_user_guide: README.md
    live_docs: [README.md, AGENTS.md, STATUS.md, PROJECT_STATE.yaml, PROJECT_DNA.yaml, PROJECT_ADAPTER.yaml, NEXT_ACTIONS.md, BACKLOG.md, WORKLOG.md, docs/EVIDENCE_LOG.md, docs/ACCEPTANCE_FREEZES.md]
