{
  "name": "@supportplane/worker",
  "version": "0.1.0",
  "private": true,
  "description": "SupportPlane background worker",
  "scripts": {
    "dev": "echo 'Worker dev not yet configured' && exit 1",
    "build": "echo 'Worker build not yet configured' && exit 1",
    "typecheck": "tsc --noEmit",
    "test": "echo 'No tests yet'"
  },
  "dependencies": {
    "@supportplane/contracts": "*",
    "@supportplane/audit": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
