# Boundary Matrix

**Backlog:** BL-102  
**Status:** strict capability boundary for future agents.

| Capability | Current state | Mock/local behavior | Real sandbox target | Production target | Allowed now? | Requires approval? | Requires secret resolution? | Requires network egress? | Evidence required | Non-claims |
|---|---|---|---|---|---|---|---|---|---|---|
| Zammad ticket read | Real sandbox read | SupportPlane API reads real ticket/customer from Zammad sandbox via HTTP with explicit sandbox labels and server-side OpenBao credential resolution. | Read deterministic Zammad sandbox ticket. | Tenant-configured production connector later. | Real sandbox read accepted. | No for read; yes for writeback enablement. | Yes, local OpenBao sandbox only. | For real sandbox, local-only. | API/browser provenance, no-token proof. | No production Zammad. |
| Zammad internal-note writeback | Real sandbox writeback | Real sandbox internal note only, approval-gated, idempotent, no public reply, no production URL. | One internal note to sandbox ticket. | Production writeback future only. | Real sandbox writeback accepted. | Yes. | Yes. | Yes, sandbox-only. | Approval, policy, worker, Zammad result, idempotency, kill switch. | No public replies, no production writeback. |
| AI draft/summary | Real sandbox call | Host-controlled Ollama provider performs real model calls to llama3.1:8b via podman0 bridge; fallback only when host/model unreachable. | Ollama local provider. | Production model governance later. | Real sandbox call accepted. | Human review required before action. | No. | No cloud egress. | Model/prompt/context hash/latency/local-provider marker; fallbackUsed=false proof. | No cloud AI, no autonomous send. Model upgrade deferred to BL-121. |
| Credential references | Local sandbox resolver | `secretRef` is redacted from API/browser; backend resolves Zammad sandbox token through OpenBao only. | OpenBao local placeholder resolver. | Vault/KMS or equivalent with rotation. | Local sandbox resolver accepted. | Admin/operator management only. | Yes, local sandbox. | OpenBao local only. | No secret in API/UI/evidence/logs/storage. | No production secrets. |
| Worker/outbox | PostgreSQL canonical + NATS bridge | Process-once/mock delivery remains; approved queue events publish to JetStream and worker can consume durable product envelopes. | NATS JetStream durable worker. | Production broker cluster later. | Local NATS bridge accepted. | Approval before queueing where policy requires. | For writeback, yes. | For writeback, yes. | Durable stream/consumer, idempotency, DLQ. | No production queue. |
| Delivery policy | Real local gate | Sandbox read allowlist, production/external URL denial, writeback denial, kill-switch denial. | Sandbox allowlist under kill switch. | Tenant-admin production policy later. | Yes, as blocker/gate. | Yes for writeback. | Depends on action. | Depends on action. | Allowed and blocked path proof. | No hidden egress. |
| Email notification | Not real | None or disabled. | Mailpit local SMTP capture. | Production email provider later. | No real email now. | Yes if tied to support action. | Future provider creds yes. | Mailpit local only. | Captured message and no internet email proof. | No real delivery. |
| Evidence bundle | Local sandbox MinIO artifact | JSON/Markdown generated and persisted to local MinIO with SHA-256 checksum; not signed or compliance-grade. | MinIO artifact with checksum. | Immutable/compliance storage later. | Local sandbox persistence accepted. | No. | No. | MinIO local only. | Object key/checksum/no-secret proof. | No compliance certification. |
| Kubernetes runtime | Cluster foundation + app/Postgres + topology real | Kind/Podman cluster `supportplane-local` with API/Web/Worker in `supportplane-app`, PostgreSQL in `supportplane-data`, OpenBao/NATS/Mailpit/MinIO/Zammad in `supportplane-integrations`. | Podman-backed local cluster with app/data/integration workloads. | Production cluster future only. | Cluster foundation + app/Postgres + topology accepted. | n/a | Local placeholders only. | Local only. | Cluster commands, namespaces, Podman backing, image load, browser mock identity, pod restart survival. | No cloud or prod deployment claim. |
| Observability | Local baseline accepted | Correlation IDs, structured logs, Prometheus metrics, local status endpoint, OTel Collector, Prometheus, Grafana, Loki-ready stack. | Production monitoring later. | Local-only observability only. | n/a | Grafana local admin secret only. | Local only. | Correlation ID logs/metrics/status; Prometheus scrape/query. | No production monitoring, alerting, retention, or cloud telemetry. |
| PBX/CTI | Local sandbox | Asterisk AMI bridge; canonical event ingestion; caller match. | Asterisk 22.8.2 local sandbox. | Production CTI later. | BL-117 accepted. | Yes for real CTI. | Likely yes. | Local/internal only first. | Internal call event, no PSTN. | FreePBX GUI deferred; no PSTN. |
| Endpoint diagnostics | Not implemented | None. | osquery/read-only diagnostics later. | Managed endpoint agent later. | No. | Yes. | Agent identity later. | Outbound-only later. | Read-only proof, no arbitrary shell. | No remediation/agent now. |
| Tauri companion | Not implemented | Web mock panels only. | Desktop app explicit sharing later. | Signed desktop app later. | No. | Operator consent required. | No. | API only later. | Start/stop sharing proof. | No desktop app now. |
| Screen/OCR/remote observation | Not implemented | Metadata-only mock observations. | Consent-gated OCR/screen context future. | Privacy-reviewed production future. | No real capture now. | Yes, explicit consent. | No. | Maybe local OCR only. | Consent, redaction, no raw retention by default. | No ambient surveillance or remote desktop. |

## Rules for Future Changes

- Do not flip a capability from mock to real without a backlog item, tests, runtime identity proof, browser/API evidence, and state-file reconciliation.
- Do not expose raw secrets in browser storage, API responses, screenshots, logs, evidence bundles, or PostgreSQL config.
- Do not claim production readiness, production auth, production secrets, production monitoring, customer-data readiness, or compliance certification from local sandbox proof.
