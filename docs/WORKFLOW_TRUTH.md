# Workflow Truth

**Backlog:** BL-102  
**Status:** Real self-hosted sandbox acceptance freeze (BL-116 accepted). All core services through BL-117 are accepted. Endpoint agent is partial (5 accepted items, real Windows pending). Tauri and screen/OCR remain planned.

| Capability | Current status | Current implementation truth | Target self-hosted service | Target milestone | Acceptance proof required | Risk level | Notes |
|---|---|---|---|---|---|---|---|
| Zammad connector | Real sandbox read + writeback | SupportPlane API reads real ticket/customer from Zammad sandbox via HTTP; credential is resolved server-side through OpenBao; sandbox-only internal-note writeback is real, approval-gated, and idempotent. | Zammad sandbox | BL-107/BL-109/BL-111/BL-115 accepted | Real sandbox read/write tests, browser provenance, idempotency proof. | High | Zammad is first real target. |
| Ticket/customer lookup | Real sandbox read | Reads deterministic customer/ticket from Zammad sandbox via real HTTP. | Zammad sandbox | BL-107 accepted | Read deterministic customer/ticket from Zammad sandbox. | Medium | No production customer data. |
| AI draft generation | Real sandbox call | Host-controlled Ollama provider performs real model calls to gemma4:e4b via podman0 bridge (10.88.0.1:11435, Ollama v0.22.0); fallback only when host/model is unreachable. | Ollama | BL-108/BL-121 accepted | Local model output with model/prompt/context hash/latency; no cloud call; fallbackUsed=false proven. | Medium | Output still requires human review and is not trusted for autonomous send. |
| AI summaries | Real sandbox call | Same Ollama provider contract as drafts; real model call proven with gemma4:e4b and fallbackUsed=false. | Ollama | BL-108/BL-121 accepted | Local summary metadata and no cloud call proof. | Medium | Same provider boundary as drafts. |
| Incoming call / telephony | Local sandbox | Asterisk AMI bridge accepts canonical events; caller match; session auto-create. | Asterisk 22.8.2 local sandbox | BL-117 accepted | Internal call event, no PSTN claim. | High | FreePBX GUI deferred. |
| PBX/CTI | Local sandbox | Asterisk AMI bridge implemented; FreePBX GUI deferred. | Asterisk/FreePBX | BL-117 accepted | Internal call event, no PSTN claim. | High | Not part of core path. |
| OIDC authentication | Accepted local sandbox | Full browser OIDC redirect/callback/login flow implemented with PKCE. Keycloak realm role mapping to SupportPlane UserRole. Service account token store with SHA-256 hashing, expiry, and revocation. MFA hook available, not enforced. | Keycloak | BL-083 accepted | Browser login, role mapping, service tokens, MFA hook. | Critical | Local auth remains the working path; production IdP future. |
| Writeback to ticketing system | Real sandbox internal note only | Real sandbox internal-note writeback to Zammad ticket via approval-gated action/outbox/NATS worker path; idempotency proven; no public reply. | Zammad sandbox | BL-111 accepted | Approval, policy, worker, Zammad result, idempotency, kill switch. | Critical | Sandbox only. |
| Email sending | Not real | No internet email; no real SMTP delivery. | Mailpit | BL-113 accepted | Mailpit captures local SMTP; no internet email sent. | Medium | Local capture only. |
| External worker/broker semantics | Local sandbox bridge | PostgreSQL outbox remains canonical; NATS JetStream bridge publishes/consumes product envelopes. | NATS JetStream | BL-110 accepted | Durable stream/consumer, retry/dead-letter, idempotency. | High | Not production broker HA. |
| Real credential resolution | Local sandbox resolver | Credential refs resolve server-side through OpenBao for Zammad sandbox only; raw secret stays backend-only. | OpenBao | BL-109 accepted | Server-side resolution and no leakage. | Critical | No production secrets. |
| Real secrets vault/KMS | Local sandbox only | OpenBao is local/dev resolver, not production secret management. | OpenBao local sandbox | BL-109 accepted | Local resolver with disable path and audit. | Critical | Production gap remains. |
| Real network egress | Sandbox allowlist | Zammad sandbox read is allowlisted; uncontrolled external and production-looking URLs are blocked; writeback blocked. | Network policy/egress guardrails | BL-115 accepted | Only local Zammad sandbox allowed; kill switch blocks. | Critical | No uncontrolled egress. |
| Production evidence/compliance | Local sandbox MinIO persistence | Evidence JSON artifact persisted to local MinIO with direct read/checksum proof; not signed or compliance-grade. | MinIO for local artifacts | BL-112 accepted | Object key/checksum and explicit local/non-compliance disclaimer. | High | MinIO alone is not compliance. |
| Endpoint agent | Partial (5 accepted items) | Local outbound endpoint agent with registration, heartbeat, inventory, fixed read-only diagnostics, Device Console UI, and audit. Real Windows runner pending. | osquery/future agent | BL-055/056/058/059/060 accepted; BL-057/118/129/130/131/132 partial | Read-only diagnostics, no arbitrary shell, real Windows proof pending. | High | Windows and broader remediation partial. |
| Tauri/operator companion | Future only | Web mock screen panels only; no Tauri. | Tauri app later | BL-119 | Explicit sharing state in desktop app. | High | Later only. |
| Screen monitoring/OCR/remote desktop observation | Future only | No raw pixels, OCR, clipboard, or monitoring. | Tesseract/PaddleOCR or remote tool later | BL-120 future | Consent-gated proof, redaction, no ambient surveillance. | Critical | Do not build before privacy design. |
| Local Kubernetes/Podman cluster | Deployed and verified | Kind/Podman cluster `supportplane-local` and four namespaces; SupportPlane apps/PostgreSQL and self-hosted services deployed. | Kind on Podman | BL-103/104/105/106 accepted | Cluster starts, namespaces exist, all workloads healthy. | Low | Verified with `kindest/node:v1.31.4`; Ollama host-controlled. |
| SupportPlane API in cluster | Deployed and verified | API Deployment + Service in `supportplane-app`; health via port-forward `localhost:4210`. | Same | BL-104 accepted | `/health` returns ok with postgres/local auth runtime identity. | Low | Built from `localhost/supportplane-api:local-k8s`. |
| SupportPlane Web in cluster | Deployed and verified | Web Deployment + Service in `supportplane-app`; reachable via port-forward `localhost:3300`. | Same | BL-104 accepted | Browser loads, header shows DEV/MOCK DATA/local auth/postgres. | Low | Built from `localhost/supportplane-web:local-k8s`; `NEXT_PUBLIC_API_BASE_URL=http://localhost:4210`. |
| SupportPlane Worker in cluster | Deployed and verified | Worker Deployment in `supportplane-app`; PostgreSQL outbox canonical with NATS JetStream bridge (BL-110 accepted). | Same | BL-104/BL-110 accepted | Worker processes outbox actions through NATS JetStream durable consumer. | Low | Built from `localhost/supportplane-worker:local-k8s`. |
| PostgreSQL in cluster | Deployed and verified | StatefulSet + PVC in `supportplane-data`; `postgres-data-postgres-0` Bound 1Gi. | Same | BL-105 accepted | Prisma migrate/seed succeed; restart survival proven. | Low | Upstream `postgres:16-alpine` image; local placeholder credentials only. |
| Object storage / MinIO evidence artifacts | Local sandbox proven | MinIO Deployment with PVC in cluster; evidence JSON artifact written and verified with direct boto3 head_object/get_object SHA-256 checksum. | MinIO | BL-112 accepted | Artifact write/read/checksum and UI metadata. | Medium | Not compliance-grade. |
| Observability | Local baseline accepted | Correlation IDs, structured API/worker logs, \`/metrics\`, \`/observability/status\`, Prometheus scrape/query, Grafana health, OTel Collector and Loki-ready local stack. | Production observability | BL-114 accepted | Local logs/metrics/status with correlation ID and no-secret proof. | Medium | Not production monitoring; Loki has no log shipper yet. |
