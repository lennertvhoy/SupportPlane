=== BL-116 Boundary Matrix Reference ===
Generated: 2026-04-30T12:08+02:00
Canonical source: docs/BOUNDARY_MATRIX.md (committed)

This freeze accepts the following boundary states:

ACCEPTED (real sandbox behavior):
- Zammad ticket read — Real sandbox read via HTTP with sandbox labels
- Zammad internal-note writeback — Real sandbox internal note only, approval-gated, idempotent, no public reply, no production URL
- AI draft/summary — Host-controlled Ollama, local provider, fallbackUsed=false
- Credential references — Local OpenBao sandbox resolver, secretRef redacted
- Worker/outbox — NATS JetStream primary + PostgreSQL fallback, durable envelopes
- Delivery policy — Sandbox allowlist gate, production/external denial
- Kubernetes runtime — Kind/Podman cluster, 4 namespaces, image load verified
- Observability — Correlation IDs, Prometheus, local stack, localOnly=true
- MinIO evidence artifact — Local sandbox object persistence proven with direct read/checksum
- Mailpit notification — Local SMTP capture only, no internet email

ACCEPTED (mock-only behavior, clearly labeled):
- PBX/CTI — Mock boundary only (no real telephony)
- Email notification — No real internet email delivery

NOT ACCEPTED / NOT IMPLEMENTED:
- Endpoint diagnostics — Not implemented
- Tauri companion — Not implemented
- Screen/OCR/remote observation — Not implemented
- Production connectors — Out of scope
- Production auth — Out of scope
- Production monitoring — Out of scope
- Compliance certification — Out of scope

Rules for future changes (from canonical source):
- Do not flip a capability from mock to real without a backlog item, tests, runtime identity proof, browser/API evidence, and state-file reconciliation.
- Do not expose raw secrets in browser storage, API responses, screenshots, logs, evidence bundles, or PostgreSQL config.
- Do not claim production readiness, production auth, production secrets, production monitoring, customer-data readiness, or compliance certification from local sandbox proof.
