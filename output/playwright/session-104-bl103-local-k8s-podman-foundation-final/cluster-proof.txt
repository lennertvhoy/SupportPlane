$ bash scripts/check_local_k8s_prereqs.sh
SupportPlane local Kubernetes prerequisite check
Status: informational only; this script does not install, create, or mutate anything.

found: podman at /usr/bin/podman - podman version 5.8.2
found: kubectl at /usr/bin/kubectl 
found: kind at /usr/local/bin/kind - kind version 0.27.0
missing: minikube
missing: helm
missing: kustomize

Podman host details:
  host.os=linux arch=amd64 kernel=6.19.13-200.fc43.x86_64 cgroup=systemd cgroupVersion=v2 rootless=true network=pasta
  Name        URI         Identity    Default     ReadWrite

kubectl current context:
  kind-supportplane-local

Kind clusters visible with KIND_EXPERIMENTAL_PROVIDER=podman:
  supportplane-local

Minikube profiles: unavailable because minikube is missing

No cluster was created. This prerequisite check is read-only.

$ bash scripts/create_local_k8s_cluster.sh
SupportPlane local Kubernetes cluster creation
Cluster name: supportplane-local
Provider: Kind with podman
Node image: kindest/node:v1.31.4
Status: creates or reuses the local sandbox cluster only.

using podman due to KIND_EXPERIMENTAL_PROVIDER
enabling experimental podman provider
Reusing existing Kind/Podman cluster: supportplane-local

+ kubectl config use-context kind-supportplane-local
Switched to context "kind-supportplane-local".

+ kubectl cluster-info --context kind-supportplane-local
Kubernetes control plane is running at https://127.0.0.1:33567
CoreDNS is running at https://127.0.0.1:33567/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy

To further debug and diagnose cluster problems, use 'kubectl cluster-info dump'.

+ kubectl get nodes -o wide
NAME                               STATUS   ROLES           AGE   VERSION   INTERNAL-IP   EXTERNAL-IP   OS-IMAGE                         KERNEL-VERSION            CONTAINER-RUNTIME
supportplane-local-control-plane   Ready    control-plane   62s   v1.31.4   10.89.3.3     <none>        Debian GNU/Linux 12 (bookworm)   6.19.13-200.fc43.x86_64   containerd://1.7.24

Next proof commands:
  kubectl apply -k infra/kubernetes/local-podman
  kubectl get namespaces
  kubectl get all -A
  kubectl auth can-i get pods -A
  KIND_EXPERIMENTAL_PROVIDER=podman kind get clusters
  podman ps --format '{{.Names}} {{.Image}} {{.Status}}'

Non-goals: this script does not deploy SupportPlane app services, Zammad, Ollama, OpenBao, NATS, Mailpit, MinIO, or observability.

$ kubectl wait --for=condition=Ready node/supportplane-local-control-plane --timeout=180s
node/supportplane-local-control-plane condition met

$ kubectl wait --for=condition=Available deployment/coredns -n kube-system --timeout=180s
deployment.apps/coredns condition met

$ kubectl rollout status daemonset/kube-proxy -n kube-system --timeout=180s
daemon set "kube-proxy" successfully rolled out

$ kubectl rollout status deployment/local-path-provisioner -n local-path-storage --timeout=180s
deployment "local-path-provisioner" successfully rolled out

$ kubectl config current-context
kind-supportplane-local

$ kubectl cluster-info
Kubernetes control plane is running at https://127.0.0.1:33567
CoreDNS is running at https://127.0.0.1:33567/api/v1/namespaces/kube-system/services/kube-dns:dns/proxy

To further debug and diagnose cluster problems, use 'kubectl cluster-info dump'.

$ kubectl get nodes -o wide
NAME                               STATUS   ROLES           AGE   VERSION   INTERNAL-IP   EXTERNAL-IP   OS-IMAGE                         KERNEL-VERSION            CONTAINER-RUNTIME
supportplane-local-control-plane   Ready    control-plane   62s   v1.31.4   10.89.3.3     <none>        Debian GNU/Linux 12 (bookworm)   6.19.13-200.fc43.x86_64   containerd://1.7.24
