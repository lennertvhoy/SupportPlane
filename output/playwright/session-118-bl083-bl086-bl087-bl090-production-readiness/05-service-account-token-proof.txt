05 Service Account Token Proof (BL-083)

Files:
- Created: apps/api/src/auth/service-account.guard.ts
- Modified: apps/api/src/auth/current-identity.middleware.ts (added redaction comment)

Key code snippet (service-account.guard.ts):
------------------------------------------------------------
const token = req.headers['x-service-token'];

if (typeof token !== 'string' || token === 'undefined' || token.length <= 10) {
  throw new UnauthorizedException({
    error: 'invalid_service_token',
    message: 'Service token invalid or expired',
  });
}

// Conceptual short-lived expiry check:
// In production this would compare tokenHash against DB and verify expiresAt.
// This slice has no persistent storage, so only format validation is performed.

req.currentIdentity = {
  tenantId: (req.headers['x-tenant-id'] as string) || 'service-tenant',
  userId: (req.headers['x-service-user-id'] as string) || 'service-account',
  userRole: 'service',
  roles: ['service'],
  permissions: [
    'support_session:read',
    'ticket:read',
    'audit:read',
    'connector:read',
  ],
  authMode: 'service',
  serviceActor: 'external-service',
};
------------------------------------------------------------

Honest status:
- The guard rejects malformed tokens (empty, 'undefined', <=10 chars) with 401.
- It sets a conceptual currentIdentity with basic read permissions.
- No database table for service accounts or token hashes exists.
- No expiry enforcement is wired; documented as a conceptual hook.
- The existing internal service-token path in current-identity.middleware.ts
  (x-supportplane-service-token) remains untouched.
