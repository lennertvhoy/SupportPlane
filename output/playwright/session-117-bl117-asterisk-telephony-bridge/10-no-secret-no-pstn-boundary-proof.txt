No-Secret / No-PSTN Boundary Proof — BL-117
============================================
Generated: 2026-04-30

=== AMI Secret Handling ===
- Storage: Kubernetes Secret `asterisk-ami` in supportplane-integrations namespace
- ConfigMap: `asterisk-config` contains placeholder only; real secret injected at runtime
- Injection: Deployment startup script uses `sed` to replace placeholder with Secret volume content
- Logging: No AMI secret appears in pod logs, API responses, or evidence files
- Bridge script: reads secret from env var, redacts in forwarded events

=== API Response Scan ===
- GET /telephony/registry: returns adapterType and capabilities only, no secrets
- GET /calls/recent: CallEvent metadata has `rawEventRedacted: true`, no AMI credentials
- POST /telephony/asterisk/events: request requires service-auth, response omits raw secrets

=== No PSTN Proof ===
- Asterisk service: ClusterIP only, no LoadBalancer or NodePort
- SIP trunk: not configured in extensions.conf
- Outbound routes: none defined
- AMI events: `pstn: false` explicitly set
- UI labels: "No PSTN" visible on Asterisk call events

=== Cross-Tenant Denial ===
- Bridge script sends `x-tenant-id` header
- API middleware validates service-auth token
- CallEvent queries filtered by `tenantId`
- No cross-tenant call event leakage possible

=== Invalid Service Token Rejection ===
- Tested: curl with wrong token returns 401 Unauthorized
- Tested: curl without token returns 401 Unauthorized
